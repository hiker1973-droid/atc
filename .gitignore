# Build artifacts
*.exe
*.exe~
*.exe.new

# Runtime artifacts
logs/
atis_cache/
launcher.log
tools/.watch-offsets.json

# Editor / OS junk
New Text Document.txt
*~
.DS_Store
Thumbs.db

# Operator-local artifacts (per-rig SRS client settings, Windows shortcuts)
*.lnk
default.cfg
global.cfg

# Claude Code per-machine state
.claude/settings.local.json

# Squadron kneeboard reference images (large binaries — keep local)
CA KNEEBOARD/
PG KNEEBOARD/
